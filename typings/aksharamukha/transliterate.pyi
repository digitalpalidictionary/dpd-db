# https://github.com/virtualvinodh/aksharamukha-python/blob/main/aksharamukha/transliterate.py

def process(src: str,
            tgt: str,
            txt: str,
            nativize = True,
            post_options = [],
            pre_options = [],
            param="default") -> str:
    ...
