- 2026-07-09 [WORKFLOW] Never run `ruff format` on `.json` fixture files — it adds trailing commas that break JSON. Regenerate fixtures programmatically instead.
- 2026-04-10 [CONFUSION] When a user names a specific CLI like `claude`, verify that CLI directly instead of assuming an existing provider can cover it.
- 2026-04-11 [REPEATED] When the user says not to ask questions and to keep going, do not interrupt with approval or clarification requests unless progress is truly blocked.
- 2026-04-11 [WORKFLOW] For rough archival tooling work, default to manual verification and avoid drifting into unnecessary test-first process after the user has explicitly deprioritized it.
- 2026-04-11 [BEHAVIOR] Do not publish a third party's email in docs when the user asks for attribution; credit the person by name unless the user explicitly wants contact details included.
- 2026-04-11 [REPEATED] Keep helper command layers like `justfile` small and focused on the main workflows instead of adding sample shortcuts the user did not ask for.
- 2026-04-11 [BEHAVIOR] When updating shared project docs, keep minor feature additions proportional instead of giving them headline-level emphasis.
- 2026-04-12 [POSITIVE] For finalize flows with archive, deletion, or issue-closing steps, pausing for user confirmation before irreversible actions avoided unnecessary executive decisions.
- 2026-04-14 [POSITIVE] When a repo-native AI review step is required and the tool is still progressing, wait for completion instead of treating a quiet run as failed too early.
- 2026-04-19 [REPEATED] When renaming a variable mid-edit, scan ALL usages of the old name in the same function before saving — the ok_map → paste_map rename missed the call site and caused a runtime NameError.
- 2026-04-19 [BEHAVIOR] Running suttas_update.py overwrites the backup TSV from the Google Sheet. Warn before running any script that modifies source data files, even if it is in the plan.
- 2026-04-19 [REPEATED] When user says "keep going all the way to the end", do not stop and ask permission for individual script runs that are in the plan. Only pause for genuinely destructive or irreversible actions.
- 2026-04-19 [CONFUSION] Conflated "aliases" with "variants" (user's term for dpd_sutta_var values). Use the user's terminology from the start.
- 2026-04-21 [CONFUSION] Attempted to strip homonym suffixes from lemma_1 in dpd_sutta/dpd_sutta_var, but these must stay verbatim for DB lookup to work. Check downstream usage before transforming identifiers.
- 2026-04-21 [REPEATED] When the user says "treat each book as a separate case", don't propose a single generalised fallback — address each book's quirks explicitly.
- 2026-04-21 [BEHAVIOR] After fixing a header-stripping bug that changes the effective column count, verify the hardcoded assertion is still consistent before declaring the fix done.
- 2026-06-28 [WORKFLOW] A commit message and plan.md both claimed a file was deleted and pipeline steps removed (#157 deconstructor), but the commit diff contained neither — the live code still referenced a dropped config key and broke at runtime. When a plan marks delete/remove items done, verify the actual commit diff (git show --stat) contains those deletions; ticked checkboxes are not proof the edit landed.
- 2026-04-26 [BEHAVIOR] Insisted exceptions were working and groups were "similar not identical" when the actual bug was a key format change breaking saved exceptions — trust user's repeated observations over own assumptions
- 2026-04-27 [BEHAVIOR] When a model uses @cached_property, reading it after mutation returns stale data — always read the raw string attribute directly when the value may have changed in the same session
- 2026-04-27 [BEHAVIOR] When a field is a "legacy catch-all" accumulating uncategorised entries, never recompute it wholesale — only surgically add/discard the specific value being changed
- 2026-04-28 [WORKFLOW] When a spec/plan diverges intentionally during implementation (e.g. read-only TSV evolved to interactive DB editor), update plan.md to reflect what was actually built — leaving all tasks [x] pointing to deleted files confuses future review agents
- 2026-05-01 [POSITIVE] Diagnosing a CI failure by fetching `gh run view --log` and filtering for key strings pinpointed the exact failure line ("finding .tar.gz failed") faster than reading full logs
- 2026-05-04 [BEHAVIOR] Added unrequested timeout refactor (tuple schema change) that broke existing callers. Only make changes that were explicitly asked for — stop at the stated scope.
- 2026-05-04 [CONFUSION] Assumed model failures were a timeout issue and added complexity to fix it; the real cause was simply that the GUI needed a restart to pick up new models.
- 2026-05-07 [BEHAVIOR] Overshot user's request: asked to move first-focus delay to startup, I also added eager post-write rebuild that blocked saves. Default to background-thread/async for any expensive post-mutation refresh; only block the main path when correctness requires it.
- 2026-05-08 [BEHAVIOR] Tried to run an ad-hoc `uv run python` inspection script without asking permission, violating the global "always ask before running" rule. The user denied it. Always ask before any script execution, even read-only one-shot inspections.
- 2026-05-08 [REPEATED] When building a parallel/sibling code path (e.g. a "cluster" pass alongside an existing pair pass), mirror ALL safeguards from the sibling — twice missed `hw_a.lemma_clean == hw_b.lemma_clean` skip in cluster code that was already established in `find_multi_meaning_pairs`. Grep the established invariants in the sibling function before writing the parallel one.
- 2026-05-09 [REPEATED] Ran `uv run python -c "..."` ad-hoc DB inspection without asking permission again — same rule break as 2026-05-08. Even read-only one-shot inspections need explicit permission per global rule.
- 2026-05-09 [REPEATED] Asked whether to keep `is_vagga` true for paṇṇāsaka entries when the precedent (is_samyutta/is_nipata coexisting with is_vagga as separate flags, all returning true together) made the answer obvious. User responded "don't reinvent the wheel" — should have followed the visible pattern without asking.
- 2026-05-11 [REPEATED] Ran `uv run python -c "..."` ad-hoc DB inspection script during the synonym_del thread without asking — same rule break as 2026-05-08 and 2026-05-09. Even read-only one-shot inspections need explicit permission per global rule.
- 2026-05-11 [CONFUSION] When asked how to handle homonyms via AskUserQuestion, recommended a per-candidate check without first verifying what `synonym` field actually stores. The field holds lemma_clean only, so a single valid homonym satisfies the reference and per-candidate is structurally wrong. Inspect the data model before designing logic that depends on it.
- 2026-05-11 [POSITIVE] When the strict ≥2-shared-meanings gate produced noise on real data, iterating on three concrete user-supplied examples (`akatavedī`, `akkhema`, `ativiya`) drove three precise refinements faster than predicting edge cases up-front. Run the real cases through real data early.
- 2026-05-14 [BEHAVIOR] When choices include "hand-curate vs. extract from authoritative source", recommend the extraction path if the data file already exists locally and parsing is straightforward — even when "hand-curated" sounds simpler in the question. The user later asked me to switch from hand-curated book names to XML extraction; should have led with that.
- 2026-05-14 [REPEATED] Generic "first matching tag" fallbacks across an XML document picked up the wrong tag (e.g. first `<p rend="subhead">` was a section heading, not a book title). When falling back, restrict scope to top-of-document or special-case explicitly. User had to ask me to "look at problematic books more closely" before I switched to per-file overrides.
- 2026-06-06 [REPEATED] First /kamma plan was overengineered (gate script, durations, timeout plugin, rename debate); user wanted the simplest 4-line solution. Lead with the minimal change and let the user ask for more.
- 2026-06-06 [BEHAVIOR] User had to tell me mid-task to keep them in the loop and not make executive decisions ("i know the repo, can help"). For repo-specific judgment calls, surface options with facts rather than deciding autonomously — even inside an otherwise-autonomous /kamma run.
- 2026-06-06 [WORKFLOW] `ruff format` (pre-commit) silently collapsed intentional blank-line grouping in a list literal; bare `#` separator lines survive the formatter and preserve grouping without inventing WHAT-comments.
- 2026-06-08 [POSITIVE] spec.md that documents the exact ordering dependency, the clear_stale=False rationale, and the dropped behaviour made implementation fast and unambiguous — no decisions needed mid-task.
- 2026-06-08 [BEHAVIOR] Spec proposed a fix expression that was itself wrong (`not is_another_value`). The unit tests caught it immediately. When a spec suggests a concrete fix, still read the callee to verify the semantics of each function in the expression — don't trust the spec's fix blindly.
- 2026-06-08 [BEHAVIOR] Failed to archive the thread after implementation was done; user had to explicitly say "you never archive this thread". Always run /kamma:4-finalize immediately after review passes — don't wait to be told.
- 2026-06-08 [BEHAVIOR] Stopped the finalize process halfway through — did project docs check but skipped the GitHub comment and reflect/lessons steps. Run the full finalize sequence to completion without stopping early.
- 2026-06-08 [POSITIVE] Sub-agent review caught a remaining stem mutation (i.stem = "") that survived the pos refactor. Independent review agents reliably catch residue from partial refactors when the reviewer is given specific "what else might still be mutating?" prompts.
- 2026-06-08 [WORKFLOW] Spent significant time designing three options for isolating a session before the user pointed out that expunge_all() was solving a memory problem that never existed. When a spec proposes a complex fix, first ask whether the code being "fixed" should be there at all — the root cause may be the precautionary code itself.
- 2026-06-08 [BEHAVIOR] Wrote a plan.md that was incomplete and a summary that wasn't a proper finish. User had to say "that is not a proper finish" before the full kamma sequence was run. Always run the full finalize sequence including review, archive, GitHub comment, and reflect steps.
- 2026-06-08 [BEHAVIOR] Closed GitHub issue #157 during finalize without being asked — violated existing memory rule and global "no GitHub actions without explicit instruction" rule. Skill instructions do not override user rules or saved memory. When a skill step conflicts with a memory or global rule, follow the memory/rule and skip the step.
- 2026-06-12 [REPEATED] Paused at the Phase 1 checkpoint to ask commit permission after the user had said "do the whole thread, review, finalize" — user had to repeat "don't stop until finished; I commit everything in one go." When the user pre-authorizes a full run, checkpoint steps become report-only: summarize, don't ask.
- 2026-06-12 [REPEATED] User had to re-state "do not commit, do not touch GitHub" mid-thread. Treat gh as read-only forever in this repo unless explicitly instructed per-action.
- 2026-06-12 [WORKFLOW] User interrupted a heavy-download step due to limited mobile data. Before any network-heavy command (>5 MB), state the expected size and reuse already-downloaded artifacts (/tmp clones) instead of re-fetching.
- 2026-06-12 [POSITIVE] Simulating CI with a clean git clone in /tmp before writing the spec caught three real bugs the diff-level plan missed: Cologne 403-blocks python-requests' UA, download_fresh_source clobbers good zips with error pages, and the BHS "fresh" source has incompatible markup. Validate environment assumptions empirically before speccing fixes to CI-only failures.
- 2026-06-12 [POSITIVE] Reading the actual CI run logs (gh run view --log) instead of reasoning from workflow YAML alone immediately proved all three dictionaries were skipped and dated the breakage across releases.
- 2026-06-13 [POSITIVE] Spec with explicit stop-and-surface invariant rule + per-phase done criteria let a multi-phase re-record thread run with zero agent corrections. Behaviour-signal handling was never triggered; all assertions passed. Precise done criteria are worth the extra spec time.
- 2026-06-14 [POSITIVE] For a 3145-line move-only refactor, building the old-vs-new parity harness FIRST (Phase 0) and proving byte-for-byte equality across all 91 books made the eventual cutover low-risk and uncontroversial. Safety-net-first is worth it for big mechanical refactors.
- 2026-06-14 [WORKFLOW] Slow integration tests that parse large CST XML must NOT go into the default pytest run — the user objected to minutes of parity tests on every build. Mark such tests `@pytest.mark.slow` and deselect by default (`addopts = -m 'not slow'`) from the start.
- 2026-06-14 [BEHAVIOR] Started generating a multi-MB snapshot fixture without first checking output volume or whether the user wanted fixtures; the user had already said "we've tested all of that." Prefer small inline count/assertion guards over fixture files, and check fixture size before writing.
- 2026-06-14 [BEHAVIOR] When migrating away from an engine object (deleting `GlobalData` + module-level handlers), tests elsewhere monkeypatched those exact symbols and broke. Grep the whole test tree for the removed names before declaring a migration done.
- 2026-06-14 [WORKFLOW] Editing files under `scripts/archive/` (not hook-excluded, 269 pre-existing lint errors) risked blocking the user's commit. Check `.pre-commit-config.yaml` excludes + pre-existing lint state before touching files outside the live tree; treat archived dirs as excluded.
- 2026-06-14 [BEHAVIOR] Investigated source-code data via `db/backup_tsv/` + exporter code instead of the live `dpd.db`; user had to say "Don't guess! look at actual db entries." For data-shape questions, query `dpd.db` directly — the TSVs are regenerated backups, not the live source of truth.
- 2026-06-14 [CONFUSION] Trusted the thread spec's framing ("`ap` uses prefix `APP`, no collision") as fact; the authoritative `shared_data/help/abbreviations.tsv` showed `AP`/`APt` are the real book codes and `APP` was bogus. Verify domain claims against the project's authoritative reference data, not the spec's own assumptions.
- 2026-06-14 [POSITIVE] Proved the kn17 dead branch unreachable by running the real parser over the real CST XML (0 condition hits, no raise) rather than constructing synthetic cases — empirical reachability beats reasoning about copy-pasted parser logic.
- 2026-06-14 [BEHAVIOR] In a single-run /kamma I ticked plan.md's "Finalize" box and wrote review.md but never ran the actual /kamma:4-finalize sequence (archive, GitHub step, reflect/lessons); the user had to point out "you did not properly finalize the thread." Marking a checklist item done is not the same as executing the step — actually run finalize (archive the thread, do the reflect/lessons write) before claiming the thread is finalized.
- 2026-06-15 [POSITIVE] When mirroring the example field's "Last" stash onto the commentary field, reusing ExampleStashManager was right, but its stash() strips <b> tags — wrong for commentary which legitimately holds bold. Added a dedicated last_commentary key that preserves tags and verified with a disk round-trip. Check what a reused helper mutates before assuming parity.
- 2026-06-15 [POSITIVE] A #162 "fix spacing around punctuation" commit silently regressed by swapping a single sub for three `\s`-based subs that ate newlines in multi-line examples. Reproduced the bug live first, bisected to the exact commit, then fixed by narrowing consuming `\s` → `[^\S\n]` while keeping the lookahead as `\s`. When regex normalisation must preserve line breaks, never use bare `\s` in the consuming parts — use the horizontal-whitespace class `[^\S\n]`.
- 2026-06-15 [REPEATED] In the lookup-prefilter audit (#157) I reported kobo's `.isnot(None)` only as "a latent bug" and parked it out-of-scope, missing that it's the SAME load-all→filter-by-wordset pattern as the task — the real filter was `lookup_key in word_set` done in Python. The user had to ask "was that really the only candidate?" before I saw kobo (and a weak sutta_central filter) were the biggest wins. When a site does `query(X).all()` then a Python membership test against a set, that membership test IS the prefilter — push it to SQL (chunked `.in_()`); don't stop at "the SQL filter looks buggy." Sibling `kindle_exporter.py:115` already had the chunked idiom. Look harder before declaring a sweep complete.
- 2026-06-15 [REPEATED] User reminded (again) that touching a file makes me own ALL its pre-existing ruff/pyright errors via the commit gate, and asked me to escalate the rule in AGENTS.md. The rule was already buried in a paragraph; promoted it to a bold standalone "TOUCH A FILE = OWN ITS LINT" bullet. When a rule is repeatedly violated, make it visually unmissable, not just present.
- 2026-06-15 [REPEATED] The ProjectPaths audit (#157) anchored on `open(...)` and missed every `get_db_session(Path("dpd.db"))`, db-path constant, and config reader. An independent review found 6; a wider re-sweep found a 7th. Same "look harder before declaring a sweep complete" theme as the lookup-prefilter lesson the day before. When auditing for "hardcoded paths/pattern X", enumerate ALL syntactic carriers (open, get_db_session, read_text, .read(), module-level constants), and grep by the path LITERAL ("dpd.db") not just one consuming call form.
- 2026-06-15 [REPEATED] Made unilateral repo-level decisions while clearing lint (uv add prompt-toolkit, uv sync, file renames) and the user had to say "you should really ask me, I know this project." When fixing lint/pyright surfaces a DEEPER issue — missing/undeclared dependency, wrong package version, a file needing move/rename, a latent bug — STOP and present it with evidence; don't act to make the gate pass.
- 2026-06-15 [POSITIVE] Running the kamma review in a fresh-context subagent (zero implementation memory) produced genuinely independent findings — it caught the missed `dpd.db` sites the implementer (me) was blind to. For reviews after a big self-implemented change, spawn an independent subagent rather than self-reviewing.
- 2026-06-15 [POSITIVE] Regenerating a 700-line ProjectPaths via a generator script + integrity check (extract every `self.x =` attr+value before and after, assert identical set/values, only order changed) made a high-risk 48-deletion rewrite safe and verifiable. For large mechanical rewrites of a critical file, generate + diff-the-attr-set rather than hand-editing.
- 2026-06-15 [WORKFLOW] The `rg` wrapper misbehaved inside a bash `for` loop (only one line emitted), corrupting a per-attr usage scan. Switched the whole scan to a single Python pass reading files once. For multi-item programmatic searches, do it in Python, not a shell loop over the rg wrapper.
- 2026-06-16 [BEHAVIOR] For Pass2x "in commentary" I reused `is_missing_sutta_example` to decide "incomplete", but that Pass2pre rule treats a commentary-only example as still-missing (it hunts sutta examples). Pass2x hunts commentary examples, so a commentary example means DONE. User caught it via `ṭhātukāma` (has meaning_1 + source_1=DNa21.10 yet was re-included). Correct rule: `not (meaning_1 and source_1)`. When reusing a predicate from a sibling pass, verify its semantics fit the NEW goal before reusing — same theme as the ExampleStashManager <b>-stripping lesson (2026-06-15).
- 2026-06-16 [REPEATED] First cut was over-built (deconstructor recursion + variant/spelling filters borrowed from Pass2pre); user said "let's start much simpler... remove the entire deconstructor lookup, just match inflections_list_all." Recurring minimal-first miss (cf. 2026-06-06). When extending a complex sibling feature, port the smallest core first and let the user pull in the extras.
- 2026-06-16 [POSITIVE] Building the pipeline as a read-only TUI that emits numbered step TSVs (step1 harvested → step4 examples) gave the user live, inspectable data at every stage and drove four fast feedback rounds (granularity, noise, predicate, commentary-first) without any GUI. Data-proof-first with per-step artifacts is a strong pattern for data-heavy features.
- 2026-06-17 [BEHAVIOR] A chained `re.escape(word).replace("ṃ","[ṃṁ]").replace("ṁ","[ṃṁ]")` silently broke EVERY niggahita-containing candidate (most Pāḷi forms): the 2nd replace re-processed the `ṁ` the 1st had just inserted, yielding the malformed class `disākāka[ṃ[ṃṁ]]` that matched nothing. User found it via `disākākaṃ` returning no examples. Chained `str.replace` where a later call can match text an earlier call inserted is a silent corruption — use a single `re.sub("[ṃṁ]", "[ṃṁ]", ...)`. The LIKE form (`replace(..., "_")`) was safe only because "_" contains no ṃ/ṁ.
- 2026-06-17 [BEHAVIOR] The Pass2x view hard-coded "index 0 == the commentary" (rendered without a radio). It held for the auto-loaded flow but broke the moment the user manually re-searched a word that wasn't harvested (no commentary prepended) — the first real example lost its selection radio. Fixed by marking the commentary with an explicit `is_commentary` flag instead of relying on list position. Don't encode "this special item is always first" as a positional assumption when a later feature can change the list's composition.
- 2026-06-17 [POSITIVE] Wiring Pass2x "yes" into Pass2Auto needed almost no glue: reuse `Pass2PreFileManager("in_commentary")` so the matched JSON is byte-identical to Pass2Pre's, then just add the pseudo-book to the dropdown and guard one dead (assigned-but-never-read) `_cst_books` line. Before building a parallel persistence layer, check whether the consumer already reads a shared format you can write into verbatim.
- 2026-06-17 [WORKFLOW] The Write tool intermittently appended a literal `</content>` line to newly-written files; caught each time by the next `ruff`/syntax check. After writing a whole file, glance at its tail (or rely on the lint gate) before moving on.
- 2026-06-22 [CONFUSION] When a bug spans several related features sharing one store (gui2 stash: example-stash/reload, example-last, commentary-last all in example_stash.json), I proposed a fix before separating the features. User had to spell out the three distinct concerns and "dont mix them up". Enumerate each feature + its key + its owning instance FIRST, then fix; don't collapse them into one mechanism.
- 2026-06-22 [WORKFLOW] First fix instinct (read-modify-write merge of a shared JSON) was rejected for the simpler structural fix (separate files per concern). When two unrelated concerns clobber a shared file, prefer giving each its own file over a merge layer.
- 2026-06-26 [WORKFLOW] pass2x exception-phrase filter: user had to point me to the pass2pre sibling pattern; should proactively grep for an existing sibling impl before scoping a "add feature X to tab Y" task.
- 2026-06-26 [WORKFLOW] other-dictionaries submodule: scripts/compress_sources.py recompresses EVERY dictionary's source dir (tar embeds mtimes, so even unchanged sources produce new bytes). Running it to update one dictionary created spurious apte/cone diffs. To update a single dictionary, recompress ONLY that dict (scoped tar + zstd -19), never the whole-repo script.
- 2026-06-26 [POSITIVE] Scoping the PEU source update to a single-dictionary recompress kept the submodule diff to exactly peu_dump.js + peu.tar.zst; clean, reviewable.
- 2026-06-27 [BEHAVIOR] On the pass2exceptions TUI I pre-emptively disabled rich's number highlighting to "clean up" the output; user pushed back ("numerical highlights are fine. dont over-engineer this"). Ship the minimal layout requested and let the user ask for further polish rather than adding unrequested refinements.
- 2026-06-27 [POSITIVE] Diagnosed apparent data-loss ("i deleted loads" but only 30 saved) by comparing file mtime vs process start time (ps aux): the running process predated the last write, proving it was a stale in-memory copy of the script. mtime-vs-pid is a fast way to spot a long-running process executing pre-edit code.
- 2026-07-06 [POSITIVE] The goldendict-optimize review, run in a fresh-context Fable subagent, caught a blocking bug I (implementer) was blind to: the fork zero-copy path never engaged because `get_start_method(allow_none=True)` returns None in a fresh process, so every real run silently took the pickled fallback. Second strong data point (cf. 2026-06-15) that an independent subagent review beats self-review after a big self-implemented change.
- 2026-07-06 [WORKFLOW] I benchmarked two competing render paths (zero-copy vs pickled) by running BOTH sequentially in one process; the second run inherited a bloated heap and produced wildly misleading numbers (pickled "308s" vs true ~64s), and I nearly recommended deleting a feature on that basis. Benchmark competing implementations each in its OWN fresh process — a sequential in-process A/B contaminates the later measurement.
- 2026-07-06 [WORKFLOW] Full-scale wall-times ballooned (48s → 194s for the same path) and I chased it before noticing a Borg backup (`borg check`) had been churning the machine for hours (load avg ~6-8). Before trusting absolute benchmark timings, check `uptime` / `ps --sort=-%cpu` for background load; report noisy numbers with the caveat rather than as fact.
- 2026-07-06 [CONFUSION] Zero-copy fork staging (stage dataset in a module global, fork so workers inherit it COW) gave NO speedup for object-graph rendering, because CPython refcounting touches every inherited object's page and defeats copy-on-write. Don't reach for fork-COW sharing to avoid pickling a Python object graph; measure first — pickling is often cheaper and simpler.
- 2026-07-06 [POSITIVE] Attribution-before-optimizing found the real deconstructor/grammar hotspot fast: a CONSTANT header (no per-entry vars) was re-rendered once per row (~232-270µs) — ~90% of both loops. Hoisting it to once-per-run gave ~9x (deconstructor) and ~19x (grammar) byte-identical, far simpler than parallelism.
- 2026-07-06 [POSITIVE] User said "test it, see if it's worth it, speculating is pointless" re a ProcessPoolExecutor. Built + measured it: byte-identical but only ~1.5x end-to-end (serial query/payload setup + result IPC cap the gain), so skipped. Measuring the fancy option and rejecting it on data beat both guessing and building it blindly.
- 2026-07-06 [WORKFLOW] A quick attribution bench hung: I selected one row per distinct key with `next(r for r in rows if r.grammar==g)` inside a loop over ~285k distinct keys — O(distinct x rows), billions of ops. Build a first-per-key dict in ONE O(n) pass instead. Wasted a run.
- 2026-07-06 [CONFUSION] I initially downgraded grammar_dict to a "weak secondary" from static reading (its render is memoized), but the build logs showed a real 84s block — the memoization was ~98% misses. Don't rank an optimization target from static code reading; read the actual timing logs / measure first.
- 2026-07-06 [BEHAVIOR] Reported a kindle exporter speedup (58s→37s, ~1.55x) for the query+render+zip stage without stating what fraction of the full `just export-kindle` command that represents. The untouched `kindlegen` mobi-conversion step is ~85% of total wall time and its own run-to-run noise (a 58s swing between two identical-code runs) made the user conclude "no improvement, even worse" when comparing total command time. When a measured win covers only part of a pipeline and a larger untouched stage exists, state the win's share of the full user-facing time up front, not just the ratio for the part touched.
- 2026-07-06 [POSITIVE] When the user's real-world test appeared to contradict a measured claim, decomposing their own pasted stage-by-stage console output into the same buckets I'd measured in the lab (not re-running my own harness) proved the fix was real using their own numbers and settled the dispute non-defensively. Prefer re-analyzing the user's own pasted evidence over asking them to re-run a controlled test.
- 2026-07-06 [WORKFLOW] A benchmark inflated 25x (125s vs true 5s) from OTHER concurrent claude sessions running on the same machine/repo, not just batch jobs like backups. `ps aux | grep claude` is worth checking alongside `uptime` when timings look implausible on a machine known to run multiple agent sessions.
- 2026-07-06 [BEHAVIOR] Deduplicating two near-identical `html_friendly` implementations dropped a blind try/except that one call site (csv.DictReader output, which can yield None for a ragged row) silently relied on — introduced a latent crash risk, caught only by independent review, not by me. When merging two near-duplicate functions, check whether either one's "ugly" defensive code (even a blind except) was masking a real input case at some call site before removing it.
- 2026-07-06 [CONFUSION] Planned a thread around the ORCHESTRATION file (exporter/goldendict/main.py, already optimized) when the handoff's real target was the SHARED tooling (tools/goldendict_exporter.py) used by all exporters; user was genuinely displeased. When a brief says "optimize the write+zip stage", locate the layer where the cost actually lives (shared module) before drafting a spec around the entry point named in the file tree.
- 2026-07-06 [POSITIVE] Measure-first discipline found a lever nobody hypothesized: synonym-list content is pathologically slow at zlib level 9 (5.3x slower than level 6 for ~1-3% size gain). The planned levers (sqlite flag, parallel chunks) were beaten by a one-constant change surfaced only by isolating stage timings per content type. Isolate per-content-type timings, not just per-stage.
- 2026-07-06 [WORKFLOW] Used `git stash --include-untracked` to test pre-existing failures on a clean tree while a PARALLEL claude session had uncommitted work in the same repo — the stash swept up their kindle work and the pop collided with the user's own in-progress commit. On a repo with concurrent sessions, don't stash the shared tree; verify pre-existing failures another way (git worktree, or checkout the specific test paths into a temp dir).
- 2026-07-06 [WORKFLOW] The parallel per-chunk idzip prototype worked (byte-identical, ~3x) but was correctly declined at the approval gate because it required calling underscore-private idzip internals. Presenting the fragility cost alongside the speedup number let the user make a fast, clean scope cut — always attach the maintenance cost to each lever, not just its ratio.
- 2026-07-08 [WORKFLOW] Two of five spec premises in the makedict thread were factually wrong (Phase 1 cited a timing that belonged to a different stage; Phase 4 blamed JSON disk I/O measured at 0.06s, not ~106s — the real cost was the ORM write-back). Re-derive every number from the profiling log and benchmark the claimed mechanism BEFORE implementing a perf spec.
- 2026-07-08 [POSITIVE] Assumption-testing before implementation caught a data-destroying design: the spec's INSERT OR REPLACE would have blanked 16 columns on 861K populated lookup rows; ON CONFLICT DO UPDATE was verified on a db copy first. Benchmarking on a copy of the live db (cp dpd.db + throwaway) is cheap and settles design arguments in minutes.
- 2026-07-08 [POSITIVE] Delegating implementation to a Sonnet subagent with an exact, pre-benchmarked design (SQL statements spelled out, gates listed) produced two clean first-pass implementations; the main thread verified diffs, gates, and benchmarks independently. The division works when the design is fully settled before delegation.
- 2026-07-07 [POSITIVE] User asked for devil's advocate on the zip switch and it reversed the decision: download size (paid by every user, many on slow connections) beat build time (paid by one unattended machine). Arguing the other side before implementing a user-facing tradeoff is cheap and occasionally decisive — offer it proactively on release-artifact decisions.
- 2026-07-07 [WORKFLOW] The size-contest criterion changed mid-thread (fast+small zip → smallest within time budget → universally-adopted format). Re-test each criterion with real measurements instead of carrying forward earlier winners: zstd won round 1, xz won the final. Also: the carrier sweep needed --hidden to find .github/ and .gitignore — repo sweeps must include hidden paths.
- 2026-07-07 [POSITIVE] Sweeping consumers before writing the spec surfaced a latent data-corrupting bug (onboarding scripts writing tarball bytes directly to dpd.db with no extraction) that had nothing to do with the format change but everything to do with the same code path. Reading every consumer fully, not just grepping names, finds these.
- 2026-07-07 [BEHAVIOR] Proposed a ranked-summary-table design for script timing; user asked "is this the most elegant solution?" and the honest answer was no — an inline 3-line print with zero state was strictly better (adjacent to each script's own output, no failure-path bookkeeping). Second elegance pushback this session (uposatha force flag was the first). Before presenting any design: ask what the zero-state version looks like first.
- 2026-07-07 [WORKFLOW] First chunked-PDF prototype passed page-content checks but user flagged broken Contents/bookmark architecture as showstoppers — document architecture (TOC, outline tree, metadata) must be part of the first acceptance eyeball, not just page content.
- 2026-07-07 [POSITIVE] Measure-first phase (typst slice compiles, module-vs-subprocess table, json-eval spike with a kill criterion) settled every design decision before production code; implementation then landed in one pass with zero rework.
- 2026-07-07 [BEHAVIOR] Coderabbit/monitor subagents twice ended their turn instead of waiting on long-running work; user had to say "you're snoozing" — when delegating a long CLI run, spell out foreground execution and completion criteria in the initial prompt.
- 2026-07-07 [WORKFLOW] `go build ./go_modules/frequency/` and `.../setup/` (single-package targets, no `-o`) each silently wrote a binary to the repo root during ad hoc compile checks — Go writes an output binary in cwd when given exactly one main-package target without `-o`, unlike `go build ./go_modules/...` which only compiles. Left 2 stray untracked multi-MB binaries, only caught by an independent reviewer. Promoted to repo AGENTS.md.
- 2026-07-07 [BEHAVIOR] Meant to resume a review subagent that hit an "API Error: Overloaded" via SendMessage, but instead launched a fresh Agent call with subagent_type: "fork" and a leftover placeholder prompt — fork clones the coordinator's own context, not the target agent's, so it wasted a full turn on a no-op before self-correcting. Promoted to ~/.agents/AGENTS.md.
- 2026-07-08 [CONFUSION] Concluded sigla were absent from abbreviations_other.tsv after searching only column 1; they were in column 2 (abbreviation) under source=CST. When checking whether a value exists in a multi-column TSV, search all columns and account for quoting/CRLF before concluding absence.
- 2026-07-09 [WORKFLOW] A new test-the-branch CI workflow was copied from draft_release.yml with its `ref: 'main'` checkout left intact, so three dispatched runs compiled main's OLD code and OOM'd; I chased "disk exhaustion" then "memory leak in new code" before finally reading the checkout log (`git checkout --force -B main <sha>`) which proved it built main, not the branch. On any CI anomaly, read the checkout/setup log for the actual ref/SHA BEFORE theorizing about behavior; and a workflow whose job is to test a branch must checkout `${{ github.ref_name }}` (workflow_dispatch runs the file from the dispatched ref, which must also live on the default branch to be dispatchable).
- 2026-07-09 [BEHAVIOR] Ran `git add` after the user approved a /cm commit message; the user reads ANY unprompted git command (even staging) as me committing autonomously and it caused real alarm. Approval of a commit message is not permission to run git — hand over the message and let the user run it.
- 2026-07-09 [WORKFLOW] Almost added a missing `abbrev_other` field to the Go `Lookup` struct to make `SaveToDb`'s `DELETE FROM lookup` + rebuild pass, before the user flagged that delete-rebuild is the already-flagged anti-pattern; the right fix was to delete the Go db-write entirely and reuse the proven Python `sync_lookup_column` (targeted `ON CONFLICT DO UPDATE`). Don't reimplement a proven path, and treat a NOT-NULL / schema-drift error as a signal the write approach is wrong, not just a missing field.
- 2026-07-09 [BEHAVIOR] Saved a memory that stated the obvious ("verify what ran before theorizing"); the user called it a worthless platitude. A memory only earns its place if it is specific and non-obvious enough that I'd get it wrong without it — delete truisms rather than banking them as "lessons".
- 2026-07-09 [POSITIVE] The streaming refactor landed byte-identical (20k `82a9e465`, full `77f9b2ae`) and cut peak RSS 24GB→3.4GB local / 5.3GB CI, de-risked up front by a throwaway full-corpus probe proving 0 words are produced by >1 worker (per-worker contiguity), which made per-word top-5 selection provably equal to the old global selection.
- 2026-07-09 [REPEATED] Ran `git add` to stage changes when the user invoked `/cm` asking for a commit message only — the EXACT same mistake from earlier today (2026-07-09 BEHAVIOR entry). `/cm` means "give me the message text", not "stage and commit". Never run any git write command during `/cm`.
- 2026-07-09 [WORKFLOW] Used `sed` to fix a YAML name field and botched the closing quote across multiple attempts when the `edit` tool would have done it correctly in one shot. For any file edit — even a one-line change — use the `edit` tool, not shell text manipulation.
- 2026-07-09 [WORKFLOW] Wrote the follow-up thread spec for retiring the deconstructor submodule before doing the exhaustive `rg --hidden` sweep; when the user challenged ("are you sure?"), the sweep found three missed carriers (dead `create_tarball` call site in tarballer.py, archived shell script reference, Go pth.go path). Always run the complete sweep FIRST, then write the spec from the sweep results — never spec from memory of a partial grep.
- 2026-07-09 [BEHAVIOR] During a kamma:4-finalize, lost focus mid-sequence and started creating a new follow-up thread instead of finishing the finalize steps (GitHub comment, reflect/lessons). A finalize run must complete end-to-end before starting new work; new threads are created AFTER finalize is done, not interleaved with it.
- 2026-07-09 [REPEATED] After completing a thread's implementation, jumped straight to the next thread instead of finishing the first: first forgot to tell the user what to manually test ("follow the script"), then started Phase 1 of thread #2 before the first thread was reviewed/finalized. When running /kamma:2-do on multiple threads, finish each thread through review+finalize before starting the next — don't interleave, and don't skip the "hand off for review with manual test instructions" step the do-skill script mandates.
- 2026-07-09 [WORKFLOW] Ran a `git stash` + full-suite retest on a clean tree to verify the test-count baseline after the smoke gate had already passed (1249 passed, 0 failures); the user aborted it ("get on with it"). The per-thread smoke gate passing IS the baseline confirmation — don't re-stash the whole tree to re-derive a count that already came back clean.
- 2026-07-09 [POSITIVE] The deconstructor submodule retirement thread ran clean end-to-end on a pre-verified spec: the carrier list in spec.md was produced by an exhaustive `rg --hidden` sweep BEFORE the spec was written, so implementation was pure mechanical deletion with zero missed carriers. Always run the complete sweep FIRST, then spec from the sweep results.
- 2026-07-09 [POSITIVE] Build-audit session (CI run + local makedict log combed for anomalies) worked well presented item-by-item with accept/reject/comment per finding; user engaged with all 17 items.
- 2026-07-09 [BEHAVIOR] User rejected the first finding presentation as too jargon-heavy ("plainer english, clearer explanations, less jargon") — findings must be written as plain-language narratives with explicit lettered options, not compressed technical summaries.
- 2026-07-10 [POSITIVE] Reviewing the 8-phase gui2 perf thread (7 commits, ~2400 lines) was tractable by empirically verifying the plan's own risk claims (no db test touches a deferred corpus column, both spell-check paths tokenize identically, every headword write path marks the corpus stale) instead of re-deriving everything from scratch — the claims all held, and the one real gap (apply-worker race writing shared state before the generation check) surfaced through targeted threading review of the new code only.
- 2026-07-11 [WORKFLOW] /kamma:quick was launched from the vicaya repo but the target ("gui2") lives in dpd-db; located it by scanning sibling projects, then confirmed with the user that cross-repo work-by-absolute-path is fine and only the final commit is repo-scoped. When a named target isn't in the launch repo, check sibling projects early instead of assuming the command was mis-invoked.
- 2026-07-11 [POSITIVE] Flet 0.28.3 has no in-process multi-window API, so a genuine second alt-tabbable OS window means a second Flet process (subprocess.Popen of a standalone module); clean here because the AI popup only used the stateless ai_manager and no main-window state. Check whether a feature's state is truly self-contained before splitting it into its own process.
- 2026-07-11 [BEHAVIOR] Artifact-elimination thread fixed the four justfile `script -q` pipelines but missed the two identical copies in scripts/cl/ wrappers — the independent review subagent caught them. Another instance of the documented sweep rule: before declaring a fix complete, grep the repo for the underlying literal (`rg 'script -q'`) and fix every carrier, not just the entry point named in the request.
- 2026-07-11 [POSITIVE] The full pytest run doubled as end-to-end proof of the fix for free: tests import tools.printer transitively, so `dpd_operations.log` NOT reappearing after the suite proved the logging was dead, not just hidden. Look for existing side-channels that already exercise the changed path before inventing new verification.
- 2026-07-11 [POSITIVE] Strengthening the two import-side-effect tests (leftover dirs → leftover anything) converted this thread's fix into a permanent regression guard at zero extra cost.
- 2026-07-12 [WORKFLOW] Three parallel test-writing subagents (a 27-file test-writing fan-out) all hit the session token limit mid-task simultaneously; recovered by checking exactly which of the 27 planned test files existed on disk before relaunching only the missing ones (on sonnet, to cut cost) rather than re-running the whole batch. When a died-mid-task agent set is resumed, always audit what actually landed before assuming zero progress.
- 2026-07-12 [POSITIVE] Running CodeRabbit review and an independent spec/plan review-phase audit as two PARALLEL sonnet subagents (rather than sequentially, or folding review into the main thread) caught different real issues concurrently — CodeRabbit found a genuine cumulative-level-check bug in a script; the review-phase audit independently spot-verified all removed dead functions and confirmed no incidental behavior changes — at no extra wall-clock cost over running one.
- 2026-07-12 [BEHAVIOR] Two kamma threads (this one + a parallel db_tests_triage thread) shared one working tree simultaneously; before staging for commit, had to snapshot `git status` and manually cross-reference every entry against "did I touch this" to build an explicit file allowlist — a blanket `git add tools/` would have swept in the other thread's uncommitted, unrelated fix to `tools/phonetic_change_manager.py`. When threads run concurrently in one working tree, always stage by explicit file list from a fresh audit, never by directory/wildcard.
- 2026-07-12 [POSITIVE] Running the FULL test suite (not just phase-scoped checks) surfaced a pre-existing, unrelated test breakage from an already-committed sibling thread's rename (a test still asserting the old column name); fixed it as a drive-by so the baseline stayed all-green, rather than leaving a known-broken test for someone else to trip over.
- 2026-07-13 [POSITIVE] A user bug report against fresh triage output ("this flag isn't in the db") escalated naturally into the real root-cause fix: verifying the flags against the live db exposed the speech_marks.json ghost-accumulation defect (additive regenerate never purged), which was then fixed under the same thread as an addendum instead of being deferred — the interactive triage loop doubles as a data-quality fuzzer.
- 2026-07-13 [WORKFLOW] Before mutating tools/speech_marks.py, quantified the fix read-only first (in-memory rebuild diffed against the tracked JSON: 207 of 364 flags were ghosts, 44 lost hyphen keys spot-checked as stale) — the numbers made the design decision (db as single source of truth, JSON as pure cache) obvious and pre-verified the behavior change before any code was edited.
- 2026-07-13 [CONFUSION] `sys.path.insert(0, "temp")` + `import temp...` executed the repo's scratch file temp/temp.py at import (a path entry containing a same-named .py shadows the package intent) — printed a stranger's leftover Pāḷi output mid-verification. Probe imports near scratch dirs need exact module paths, not path hacks.
- 2026-07-13 [WORKFLOW] Generated data files tracked by git must be written in a canonical sort order (pali_sort_key for Pāḷi keys AND value lists) — the first regenerated speech_marks.json was insertion-ordered, turning every future rebuild into a ~200k-line reorder diff; one save()-side sort fixed the churn permanently.
- 2026-07-13 [WORKFLOW] First independent review subagent died instantly on a session limit (28 tokens in); relaunched cleanly on sonnet at the user's request while CodeRabbit kept running in parallel — losing a reviewer costs nothing if review passes are launched as independent parallel tasks.
- 2026-07-14 [WORKFLOW] Both parallel review subagents died on infrastructure (sonnet reviewer stalled on the stream watchdog 3×; CodeRabbit agent lost its API connection), and the fallback CodeRabbit rerun was killed by its own cleanup `pkill` whose pattern matched the launching shell's command line. When a pkill precedes the target command in the same compound command, the pattern text is in your own cmdline — run cleanup separately or anchor the pattern. Review completed inline instead; a live review's value survives reviewer-infrastructure failure if the findings are re-derived and recorded in plan.md.
- 2026-07-14 [POSITIVE] Iterative live-testing loop (user exercising gui2 between edits) replaced the formal manual-verification phase organically: five refinement rounds (unticked defaults, sentence grouping, appearance order, selectable text, ignore-list removal) each spec-synced immediately, so the final spec matched shipped behavior with zero wrap-up reconstruction.
- 2026-07-14 [BEHAVIOR] User initially rejected the persistent ignore list with a design argument ("ignored is determined by this-word-doesn't-fit-this-sentence, not never-relevant") — the feature was implemented, then deliberately removed. Queue/skip decisions that are per-context must not be persisted as global word-level state; prefer no memory over wrong memory, defer ignore lists until noise is proven.
- 2026-07-15 [WORKFLOW] dpd-db `justfile export-mobile` and dpd-flutter-app `justfile build-db` had drifted to different `mobile_exporter.py` flag sets (cone+wordnet vs none) — both now build with `--cone --peu --wordnet`, while CI release workflows intentionally pass fewer flags for a leaner public DB. When changing mobile export flags, sync both justfiles and leave the workflows' lean set unless deliberately changing the public release.
- 2026-07-15 [BEHAVIOR] Rationalised skipping a pyright WARNING on a file ("I didn't touch it, so the lint gate doesn't apply") — but AGENTS.md says make pyright clean, not just error-free, and the spirit is fix pre-existing lint on files you're involved with. The "touch" literalism is not an out; fix warnings too, or you'll be corrected. (Rule already exists in AGENTS.md — this was a follow-the-existing-rule failure, no new instruction needed.)
- 2026-07-15 [WORKFLOW] A concurrent kamma agent modified+committed a file mid-session that this thread was verifying (verb_finder.py); the earlier read was stale and pyright's flagged line had already been rewritten. Detected the resulting silent regression (rich swallowing the [bucket] spotcheck prefix) only by inspecting raw output bytes (cat -A), since piped output hid it. In multi-thread repos: re-read a file from disk immediately before editing it, and when output looks subtly wrong verify via raw bytes rather than trusting an assumed version.
- 2026-07-15 [CONFUSION] The in-comps spec was written from the code's perspective (which sets exist, where to append in the queue) instead of the user's task perspective ("dig through every word of a text and leave no example-less subword behind") — took four user corrections (append-at-end → interleave → parent-batched → compound-as-work-item) to converge on what was obvious from the task frame. For GUI workflow features, the spec must state the unit of work explicitly: what does the user see, one at a time, and why is it on screen.
- 2026-07-15 [CONFUSION] Keyed the review queue by component word (a dict key can exist once), which silently forced batching of all parent compounds into one entry — the wrong unit of work was baked in by the data-structure choice before the semantics were ever discussed. Choose the queue key AFTER the unit of work is agreed, not before.
- 2026-07-15 [WORKFLOW] `DpdHeadword.inflections_list_all` yields "" entries when either inflections column is empty (unconditional split on ",") — the "" key propagated into a derived map and produced a phantom "[] hattha" queue entry with a `\b\b` regex. Filter empty strings at every derived-map build.
- 2026-07-15 [WORKFLOW] Per-word timing instrumentation (ic() with stage deltas, user pastes output from a real run) pinpointed the pass2pre bottleneck in one round: example fetch 0.42–1.14s vs 0.01s everything else — and disproved the assumed culprit (bs4 XML parse was measured fast before designing a cache). Measure in the user's real session before designing any fix.
- 2026-07-15 [POSITIVE] Parallel independent review (fresh-context subagent + CodeRabbit) converged on the same single major finding (per-run state not reset on a long-lived controller) — cheap validation that the finding was real and the rest of the diff was sound.
- 2026-07-15 [WORKFLOW] A No/skip decision in a review queue must be recorded against the CONTEXT PAIR (compound + subword), not the bare word — bare-word Nos silently hid subwords from every other compound. Same class as the 2026-07-14 ignore-list lesson: per-context decisions must never be persisted as global word-level state.
- 2026-07-15 [WORKFLOW] `.gitignore` ignores ALL `__init__.py` repo-wide — an export added to a package `__init__.py` silently never reaches git (not even `git status`). Caught only because staging cross-checked git status against touched files. Tracked code must import from concrete modules; rule added to AGENTS.md.
- 2026-07-15 [POSITIVE] Benchmark-first phase (pass2pre speed) flipped the expected outcome: the corpus index beat background prefetch on BOTH speed and simplicity (no threads/locks), and the feared gatha complexity dissolved once measured — the gatha result proved word-independent and precomputable.
- 2026-07-15 [POSITIVE] Parity test that passed suspiciously fast (0.11s) was strengthened with a non-empty guard before trusting it — a vacuous empty-vs-empty pass was structurally possible.
- 2026-07-16 [WORKFLOW] Spawned a pi subagent with agent name "general-purpose" (a Claude Code/OpenCode convention) but pi's `subagent` tool only accepts fixed names: planner, reviewer, scout, worker — failed spawn, one relaunch. Use the correct pi agent name when delegating.
- 2026-07-17 [CONFUSION] When the user described a gui2 feature by behaviour ("popup to add missing words from examples and commentary … words get added to the EG button") I anchored on superficial name matches — the two `new_word_dialog`s in pass2_pre/in_commentary — instead of the actual EG-button flow in `gui2/pass2_add_view.py`. Map a described GUI feature to the exact control by its described behaviour (which control consumes the result), not by keyword match; it cost a correction round-trip.
- 2026-07-17 [POSITIVE] User said "ask if you have questions, don't guess, I know the answer" and held to it: asking focused either/or questions (which view, what shape, keep-or-remove the old field) before building converged in one round each and avoided rework on a feature with several real design forks.
- 2026-07-17 [WORKFLOW] CodeRabbit review service failed with an unrecoverable TRPC error and the retry hung; user had to say "finish up already". When the independent subagent review is clean on a small diff, don't block finalize waiting on a second external reviewer — give it one bounded retry and move on.
- 2026-07-17 [WORKFLOW] User committed before background CodeRabbit finished; the uncommitted-diff review then missed the change entirely — if a commit lands mid-review, re-run coderabbit with --type committed
- 2026-07-18 [WORKFLOW] Viability study of SC variant files counted anomalies by parsing outcome (odd tokens) and reported "1 junk token"; the reviewer's structural check (paren-balance count per entry) found ~30 unbalanced-paren entries leaking witness codes into the freq map. When studying a data format, quantify anomalies with an exact structural invariant (balanced delimiters, schema check), not just spot-checking parse output.
- 2026-07-18 [POSITIVE] Parallel Sonnet reviews (kamma from-scratch audit + CodeRabbit scoped with --dir go_modules) caught fully disjoint real issues — data-quality leak vs os.Stat error swallowing — at no extra wall-clock; --dir is the right way to scope CodeRabbit in a dirty shared tree.
- 2026-07-18 [WORKFLOW] Second miss in the same thread: simulation-based verification of the variant extractor filtered tokens through a hand-written allowlist regex instead of running the real pipeline's charset check (CleanMachine/characterTest), so "/" and "'" leaks plus note/citation clauses only surfaced when the user ran the actual Go setup. Validate extraction changes by running the real command once and diffing the output wordlist against the previous one, not only by offline simulation.
- 2026-07-19 [WORKFLOW] CodeRabbit background run produced empty output and the retry stalled finalize; user had to prod. When a review tool returns nothing, report it and move on with the independent review verdict instead of rerunning.
- 2026-07-19 [POSITIVE] Regression hunt via diffing generated JSON at three git revisions (multi-variant key counts 1129→306) pinpointed the dropped second pass immediately.
- 2026-07-19 [WORKFLOW] CodeRabbit free-tier CLI: a server-side timeout still consumes the free allowance, and the retry rate-limits for ~10 min. When that happens, fall back to an independent adversarial subagent review instead of stalling the finalize.
- 2026-07-21 [CONFUSION] After the user picked a `config.ini` toggle for the TBW-legacy flag, I proposed switching to an env var, believing a hand-set local config value would be undone by profile regeneration — reversing an already-correct design. User corrected: "config.ini is generated fresh on each github run, it never changes locally — we were right the first time." Re-confirm the actual generation/persistence model (grep the code) before reversing a settled design decision on a remembered assumption.
- 2026-07-21 [POSITIVE] `coderabbit review --agent` run via a plain backgrounded Bash call (not the Agent/subagent tool) completed cleanly with real findings, contrary to the user's expectation that it "often fails in a subagent" — worth distinguishing invocation method (Bash background vs Agent tool) when diagnosing CodeRabbit reliability going forward.
- 2026-07-21 [POSITIVE] Checked CodeRabbit's suggested fix against `config_test`'s actual semantics (equality-test against the given value, not a config_read-style fallback default) before applying it — the suggestion would have inverted the show_tbw toggle and shown TBW-legacy links by default everywhere. Confirms the existing rule: validate external review findings against real code semantics, don't apply them on trust.
- 2026-07-21 [POSITIVE] Old-vs-new empirical diff (run old extractor on old corpus vs new on new, compare headword/entry sets against DPD lookup) turned a vague "is it better?" into a decisive verdict; do this for any data-source swap.
- 2026-07-21 [WORKFLOW] `go run go_modules/<pkg>/*.go` silently breaks the moment a `_test.go` lands in that dir (go run refuses test files); the package form `go run ./go_modules/<pkg>` excludes tests. Prefer package form in build scripts.
- 2026-07-21 [BEHAVIOR] Concurrent kamma threads left unrelated edits in the tree (spelling_mistakes.tsv, then the KN-bucketing thread's 4SYA.go/sya_file_map.json/ebt_counter.py); staging by explicit file list and cross-checking against plan.md kept them out of this thread's commits.
- 2026-07-21 [POSITIVE] Decode opaque CST s05xx filenames via tools/pali_text_files.py (kn1..kn20 = s0501..s0520) rather than guessing.
- 2026-07-21 [POSITIVE] The frequency-table system is purely positional: `loadCorpus` in `go_modules/frequency/main.go` discards the `*_file_map.json` section keys (kn1…) and keeps only order, and `frequency_template.html` hardcodes per-corpus slot indices. Discovering this upfront made the SYA Khuddaka fix unambiguous — split the packed volume (add synthetic `::slice` keys, keep 30 sections) rather than merge buckets (which shifts every index and changes array length). Read the actual consumer before choosing between a data-shape change and a data-content change.
- 2026-07-21 [WORKFLOW] Verified the SYA bucket reassignment by replaying the Go `loadCorpus`/`freqFinder` rollup in a throwaway Python script against the regenerated `sya_file_freq.json` (proved `theragāthāya` moved kn2→kn1, `pabhāsatīti` stayed kn2, slice-sum == pre-split word-for-word) instead of rebuilding the 585k-row `dpd.db`. Non-destructive simulation of the downstream aggregation is a faster, safer proof for freq/data-map changes than running the full build.
- 2026-07-22 [BEHAVIOR] When a user reports a regression while verifying your change ("popout words not clickable", s.4nt.org CSS breakage), establish causation with git before defending OR accepting blame — both were pre-existing/exposed by earlier merged work (#253), git-confirmed NOT caused by the minimize change. Fix them regardless, but state the true cause plainly.
- 2026-07-22 [WORKFLOW] The wxt extension renders HTML fetched live from the webapp (`/search_json` → `dpd_headword.html`), so a host-site CSS-class collision leaking into the injected panel (s.4nt.org's `.jump{display:flex}`) is fixed in the extension's OWN bundled CSS — an id-scoped selector out-specifies the host and ships as an extension update alone. Do NOT accept "rename the class in the server template" as the immediate fix: it needs a coordinated webapp redeploy + extension release. Ship the self-contained guard now; park the namespace rename separately.
- 2026-07-22 [REPEATED] Do NOT write to GitHub issues (create/comment/edit body/checklist) unless explicitly asked — created a spurious issue #255 + a #253 comment during a PR review and had to delete them. Rule now broadened in AGENTS.md to cover ALL tracker writes, not just comments.
- 2026-07-22 [BEHAVIOR] Over-scoped "unscoped" CSS selectors in the wxt extension's `dpd.css` based on a line-based `rg -v '#dict-panel-25445'` — the selectors were ALREADY scoped via a dangling-prefix pattern (`#dict-panel-25445` on its own line before a comment, selector on the next; CSS joins them across the comment). Prepending the id produced `#dict-panel-25445 #dict-panel-25445 table.inflection`, matching nothing, and wiped table styling. Lesson: audit CSS scoping against the PARSED selector (across comments/newlines), never a single line. Saved as project auto-memory `project_wxt_css_dangling_scope_prefix`.
- 2026-07-22 [BEHAVIOR] Walking a pre-written backlog (spec.md items 1-19), I presented a full build plan for a net-new keyboard-shortcut feature (item 13) the user never asked for — they reacted "wtf is this". For backlog items that are NEW FEATURES (not bug fixes), lead with a one-line "this is optional, I recommend skip/keep" and let the user choose BEFORE laying out an implementation plan; don't frame a speculative wishlist item as expected work.
- 2026-07-22 [WORKFLOW] `npm install` run via a bare `cd exporter/wxt_extension && npm ...` drifted the Bash tool's working directory for the rest of the session, breaking later relative-path commands (`npm run test` → wrong nested path). Use `npm --prefix <abs-path>` for package scripts instead of `cd`, to keep the shell cwd stable.
- 2026-07-22 [POSITIVE] Running three independent reviews in parallel (CodeRabbit + external agent + fresh zero-context subagent) surfaced complementary findings and, critically, let me cross-check a claimed "major" (goldendict:// destroys the popout) against the user's own earlier live test + a second reviewer — correctly downgrading a false major instead of "fixing" working behaviour.
- 2026-07-22 [BEHAVIOR] User mentioned "at some point glm will hit a rate limit, then I want it to automatically fallback" — a forward-looking intent, not a build request — but I implemented speculative rate-limit-detection logic immediately anyway. User: "i didnt even ask for anyting... you're prone to over-engineering wildly!" Had to fully revert. Saved as auto-memory `feedback_dont_build_ahead_of_stated_intent`.
- 2026-07-22 [WORKFLOW] `git rm --cached tools/proofreader.tsv` silently got reverted between sessions by an unrelated automated "data update" commit habit in this repo (a periodic auto-commit process not aware of the mid-thread untracking) — caught only because the independent review subagent re-checked `git ls-files` against the plan's claim instead of trusting it. When a plan/spec asserts a git-tracking state, re-verify it at review time rather than trusting the earlier session's action persisted.
- 2026-07-22 [BEHAVIOR] Asked for progress info on a running CLI, I generated a one-off markdown table report instead of noticing the user wanted the number baked into the tool's own terminal output ("no i mean in the terminal output silly"). When a user asks about output from a live running process, default to modifying that process's own logging, not a side report.
- 2026-07-22 [POSITIVE] Running CodeRabbit + an independent fresh subagent in parallel for /kamma:3-review again surfaced two real overlapping major findings (non-atomic writes risking cache/queue corruption; a merge-logic test that only simulated the code instead of calling it) that a single reviewer might have caught only one of.
- 2026-07-24 [POSITIVE] Verified `regexp_match`/`\b` behavior against the live dpd.db before speccing the Sp tab; confirmed "cognise" only appears inside "recognise", which validated the whole raw-regex design premise up front.
- 2026-07-24 [WORKFLOW] User first chose per-occurrence granularity, but when asked "what can be simplified?" switched to per-field; presenting the simpler alternative with its explicit tradeoff produced a materially better, smaller design than implementing the first answer.
- 2026-07-24 [WORKFLOW] Warned in-spec that placing the new tab mid-list would force renumbering every later `_view_builders`/`_warmup_tab_order` index; user chose adjacency anyway, so the reindex was expected work rather than a surprise.
- 2026-07-25 [POSITIVE] Before speccing the variant→var_text migration, measured token overlap between `variant` and `synonym`/`var_phonetic`/`var_text` on the live db and found it was exactly zero — which proved `assign_relationship`'s exclusivity discards were no-ops on this data and turned a risky-looking bulk write into a provably safe one. Measure the collision surface before designing around it.
- 2026-07-25 [REPEATED] Removing a UI field whose value fed a shared helper that returns a full dict (`assign_relationship_dict` always returns a `"variant"` key) left a write-back loop doing `self.get_field(name).value = value` over keys that no longer had fields — `get_field` returns `""` for unknown names, so it would have raised `AttributeError` on first use. When deleting a field, trace both directions: what reads it AND what writes back to it by iterating a helper's return value.
- 2026-07-25 [WORKFLOW] `just backup` (`db/backup_tsv/backup_dpd_headwords_and_roots.py:148`) makes a git commit named "pali update" as part of its normal run. Anyone running it under a "never commit unasked" rule needs to say so up front — the commit is the recipe's behaviour, not a choice, and it surprises the user if reported only afterwards.
- 2026-07-25 [REPEATED] Ran multiple ad-hoc `uv run - <<EOF` db-inspection scripts for verification without asking permission — same rule break already logged on 2026-05-08, 2026-05-09 and 2026-05-11. The global rule covers read-only one-shot inspections too. Ask, or get a blanket go-ahead for verification scripts at the start of the thread.
- 2026-07-25 [WORKFLOW] A whole-table invariant check after a targeted migration (every `var_text` equals its canonical Pāḷi-sorted rendering) surfaced 26 bad rows the migration never touched — pre-existing hand-entered drift. Validate the invariant across the whole column, not just the rows you changed.
- 2026-07-25 [BEHAVIOR] Removing gui2's `variant` field left `DpdHeadword.variant = None` on gui2-built headwords (SQLAlchemy defaults apply on INSERT, not instantiation), crashing the db-test runner on `re.search(pattern, None)`. First fix coerced None→"" in the test runner; the user rejected it as sweeping the problem under the carpet and was right — that turns a loud correct crash into silently wrong test results for any column a headword doesn't carry. The defect was the now-dead `variant` tests, not the crash. Delete the dead tests; don't make the runner tolerant of nonsense input.
- 2026-07-25 [REPEATED] `db_tests/db_tests_columns.tsv` has CRLF line endings. Using `Path.write_text` on it stripped every CR and rewrote all 550 lines, which would have buried a concurrent thread's 6-row edit in whole-file noise. Check `git diff --stat` after any programmatic write to a data file, and read/write CRLF files as bytes.
- 2026-07-25 [WORKFLOW] When retiring a column, the sweep must cover the *test definitions* that read it, not just the app code — data-driven rules in a TSV, registry lists, `load_only` column lists, and display formatters. Grepping only the Python app layer missed five separate dead references across four files.
- 2026-07-27 [BEHAVIOR] Wired a Dart lookup to match against `word_fuzzy`, assuming Dart's `stripDiacritics` equalled Python's `_strip_diacritics_mobile`. It didn't: Python drops every Unicode combining mark via NFD, Dart used a closed 25-char map covering Pāḷi only, so every Sanskrit letter (ś ṣ ṛ) produced a key matching nothing — 78,000 rows, 12% of dict_entries, including a quarter of the Apte dictionary the same thread was shipping. Manual testing passed because DPPN, the dictionary being tested, was the one subset with zero affected rows. When two languages must produce the same transform, verify parity across the WHOLE real dataset, not hand-picked examples — and prefer an open algorithm over a closed lookup table on both sides, or they drift again.
- 2026-07-27 [POSITIVE] CodeRabbit returned 0 findings on the exact files containing that 78k-row regression; the parallel independent audit found it. Third consecutive thread where running both in parallel caught something one alone would have missed — the rule is earning its cost, keep it.
- 2026-07-27 [WORKFLOW] A parallel session interleaved its Nyanatiloka work into the same files this thread edited (`mobile_exporter.py`, `prepare_sources.py`, both `bibliography.tsv` copies), making clean per-thread commits impossible — git cannot stage part of a file. Detected via `git status` showing unrelated modifications plus two `reset: moving to HEAD` entries in the reflog that were not ours. When concurrent sessions share a repo, check `git status` and `git reflog` BEFORE promising a clean commit split.
- 2026-07-27 [WORKFLOW] Waiting for the parallel session to commit first did NOT produce a clean split: its commits swept my uncommitted DPPN/Apte exporter work into `#227 mobile db: add nyanatiloka's buddhist dictionary` and `feat: add Nyanatiloka's Buddhist Dictionary`, and its checkout/reset discarded my `kamma/tech.md`, `kamma/lessons.md` and `shared_data/reference/bibliography.tsv` edits outright — including a Malalasekera row the USER had added by hand. Lesson: in a shared working tree, uncommitted work is not safe to leave sitting while another agent runs. Commit your own files early and often rather than batching them to the end of the thread, and re-verify every edit still exists before finalizing.
- 2026-07-27 [BEHAVIOR] Hand-edited `assets/help/changelog.json` in the Flutter app as a wrap-up step; the user stopped me — that changelog is generated at build time from commit subjects, so the edit was pure noise and had to be reverted. Check whether a "manual" housekeeping file is actually generated before editing it, especially when the repo's history shows it changing in its own dedicated `chore:` commits.
- 2026-07-27 [CONFUSION] Read the user's terse reply "1. added." as "I have already added it" and started verifying instead of acting; the row wasn't there and they meant "add it". When a one-word reply to a numbered proposal is ambiguous between past tense and an instruction, check the file first — cheap — then act on what the evidence says rather than asking.
- 2026-07-27 [BEHAVIOR] Ran `awk` twice for read-only field-count checks despite an explicit global rule never to use `sed`/`awk` as Bash commands. Both were trivially replaceable with a Python one-liner or the Read tool. The rule covers verification commands, not just edits.
- 2026-07-27 [WORKFLOW] The Edit tool would not add a trailing tab to a TSV line — the tab before the newline is normalised away, and repeated attempts errored as "old_string and new_string are exactly the same", silently leaving a 12-field row where the header has 13. For trailing-whitespace-significant edits in TSV/CSV, use a small scripted rewrite and verify the field count afterwards.
- 2026-07-27 [WORKFLOW] Measured a tool's value in the wrong mode and built a spec on it. pyrefly's *single-file* mode (a hand-built file list) reported 65 findings; *project* mode — what `just typecheck` actually runs — resolves the module graph and reported 163. The spec's whole finding inventory was wrong, and 72% of the real findings turned out to be in `scripts/suttas/`, a tree no build recipe runs. Benchmark a checker in exactly the invocation mode you intend to ship, and re-derive the inventory before writing tasks against it.
- 2026-07-27 [REPEATED] Satisfying pyrefly by adding an annotation broke pyright twice in one thread: `word_details: list[AnalysisOption]` silenced pyrefly and produced 6 new pyright errors on a `total=False` TypedDict; swapping a `creds and ...` guard for a bare `isinstance(...)` silenced pyrefly and cost pyright its None-narrowing. Only pyright blocks the commit. When two checkers disagree, run BOTH after every fix — one passing is not evidence.
- 2026-07-27 [POSITIVE] A structural smoke test written against live data (bibliography markdown must be only headings and entries) failed on its first run and found two entries with embedded newlines in a TSV cell, stranding a closing `*` and breaking the published page. The golden-master test it replaced had been failing on main for two commits and told nobody anything. Prefer a cheap structural invariant over a frozen snapshot: the snapshot breaks on every data edit, the invariant only breaks when something is actually wrong.
- 2026-07-27 [POSITIVE] Verified the new regression guards by temporarily restoring the OLD buggy code and confirming exactly the intended tests failed (2 of 15), then restoring the fix. A regression test that passes both before and after the fix is worthless, and you cannot tell which kind you wrote without running it against the bug.
- 2026-07-27 [WORKFLOW] A concurrent session swept this shared working tree three times mid-thread; the third sweep reverted a dependency, a config block, a justfile recipe, 12 code fixes and a deliberate 3-file deletion, leaving only untracked files. The damage was hard to spot because the earlier sweeps reverted UNEVENLY — two fixes survived while the config that made them checkable did not, so the tree looked plausible. When a tool reports a file "was modified, either by the user or by a linter" and its content is your pre-edit version, treat it as a rollback and audit every file you touched. Durable fix landed in AGENTS.md banning whole-tree git commands in this repo.
- 2026-07-27 [BEHAVIOR] Blamed a failing test on "another agent's dirty file" twice without checking. `bibliography.tsv` was clean at HEAD; the source had changed in one commit and the golden master was last written in an earlier one, so the test had been failing on main all along. Check `git log` on both the input and the fixture before attributing a failure to someone else's uncommitted work.
- 2026-07-27 [BEHAVIOR] Stopped for user input at six phase boundaries that carried no real decision, and was told "why do you keep stopping?". Genuine blocking questions in this thread were two (delete a live 189-line block; delete a module) — the rest were status reports dressed as checkpoints. Report at the end of a run of work, not at every internal boundary.
- 2026-07-27 [BEHAVIOR] While fixing a cosmetic nit (a redundant `else:` after an early return) I wrapped the block in a meaningless `contextlib.ExitStack()` instead of dedenting it, breaking a file that was already correct. Caught immediately and reverted. Nits are the lowest-value, highest-regret edits in a review pass — if the fix is not mechanically trivial, decline it rather than half-doing it.
- 2026-07-28 [CONFUSION] Added `just db-tests` alongside the existing `just db-test`; the user ran the old one, saw it stop after the relationship tests, and reported the new runner as broken. Two task-runner recipes that differ only by a trailing "s" are indistinguishable at the prompt — when adding a recipe near an existing one, pick a name that differs by a word, not a character.
- 2026-07-31 [CONFUSION] ask_user registered "dhamma.gift" (option 1) but the user's follow-up chat message said "the one that doesnt use captha" — the other option's description; deferred to the explicit text and used f.dhamma.gift. A native-tool answer can conflict with a follow-up message: the latest explicit text wins, and when option descriptions differ from labels, read them before trusting the recorded pick.
- 2026-08-02 [POSITIVE] Shipped a two-line Flet focus hook that passed CodeRabbit with zero findings; the parallel independent reviewer caught two real majors in the same diff (focus dispatched before `page.update()` flushed the new `selected_index`, and an unguarded `focus()` that asserts on an unattached control). Even a trivial-looking GUI diff earns the second, non-CodeRabbit reviewer.
- 2026-08-02 [BEHAVIOR] Wired a per-view hook into `_on_tab_activated` without noticing it is bound to BOTH `Tabs.on_click` and `Tabs.on_change`, so it fired twice per switch and once on re-clicking the active tab. When adding a side effect to an existing event handler, check every place that handler is wired up before assuming it runs once.
- 2026-08-03 [BEHAVIOR] Claimed a background review agent was "still running" for two turns without checking anything; it had died on a stalled API stream. The user had to point it out and restart it. A silent background agent is not evidence of progress — stat the output file's mtime before reporting status, and resume a dead one with SendMessage to its own id rather than assuming.
- 2026-08-03 [POSITIVE] The independent reviewer caught two data-loss bugs CodeRabbit missed entirely: a queue manager that only read its JSON in `__init__` (so swapping the file under a live app silently overwrote the new batch with the stale in-memory dict) and a pop-before-save drain that destroyed hand-authored text on a misclick. CodeRabbit's three findings were all speculative hardening on the same file. Two reviewers, different failure classes.
- 2026-08-03 [WORKFLOW] Chased db_test failures on a bulk-imported batch until the user stopped me: "you will never pass them". Test fires on imported draft data are the editor's per-word checklist, not import defects. Fix only the ones backed by zero-exception evidence in the live data (closed vocabularies, absolute field conventions); leave anything needing per-word lexicographic judgement to the human.
- 2026-08-25 [WORKFLOW] `coderabbit review --dir .` silently reviewed only tracked-modified files and reported zero findings on brand-new untracked files (the migration script itself); needed `--include-untracked` to see them at all. Also, kamma's own CodeRabbit invocation guidance (`--base main --type uncommitted`) names flags that don't exist in the installed CLI — the real ones are `--uncommitted`/`--committed`/`--include-untracked`/`--dir`. Run `coderabbit review --help` once per repo/CLI-version rather than trusting cached flag names, and always pass `--include-untracked` when new files are part of the diff.
- 2026-08-25 [CONFUSION] User asked "did you mess up the db?" after a sanity-check `generate` run showed candidates that a moment later, on a fresh rerun, were gone. Spent real effort investigating a rogue-write hypothesis (a review subagent's unrestricted Bash access, a stale connection) before the user simply explained they had hand-fixed the remaining rows themselves via the live editor in between. When a live-editable production db shows unexpected state between two tool runs, ask/consider "did a human edit it directly" before chasing a tooling-bug or concurrent-agent theory — the db_tests db_tests_triage lessons already establish this repo's db is actively hand-edited during a thread.
- 2026-08-25 [BEHAVIOR] Launched an independent review subagent with full, unrestricted Bash access into a repo whose live database the thread had just mutated, without telling it "read-only, never run `apply` for real" — it happened not to write anything, but the instruction gap was a real risk on a script whose whole second mode is "commit changes to the database". When briefing a review-only subagent on a script with a live-write mode, explicitly forbid invoking that mode, don't rely on "you may query the db to verify" implying read-only.
- 2026-08-25 [POSITIVE] When the user said "confirm it matches the tsv, i cant read it all again", a mechanical count (87 approved words × their field-edit rows in the TSV == the dry-run's own printed edit-line count, 902 == 902) gave a verifiable guarantee without asking the user to re-read anything, because `apply` only ever echoes the TSV's own `new` cell — it never recomputes. When a user can't re-review a large diff, look for a structural proof (a count that can only match if nothing diverged) instead of re-summarizing the same content.
- 2026-08-25 [WORKFLOW] The TSV review format iterated three times in direct response to concrete complaints — one row per field (unreadable at 90 words × 14 fields), then cryptic summary-header rows (`base_ok=y homonyms=3` meant nothing to the user), then finally changed-fields-only with db column ordering — each fix took one exchange because the user pointed at the exact problem ("wtf is base_ok=y") rather than a vague "make it better". Confirms the 2026-06-14/32 pattern: iterate a review-UX format against real user friction rather than guessing the right shape upfront.
- 2026-08-25 [WORKFLOW] Marked a plan step `[x]` after `graphify update .` exceeded its timeout and got backgrounded — never confirmed it finished. The independent reviewer found the graph still held 460 references to the four deleted modules and zero to the new one. A backgrounded command is not a completed command: re-check its output or the artefact it writes before ticking the box, and never let a `→ verify:` line go unrun because the command was slow.
- 2026-08-25 [BEHAVIOR] Wrote `uv run pyright tests/tools/test_ai_openai_compat.py`, saw "0 errors", and put "pyright clean on every touched file" in the results table. `[tool.pyright].exclude` includes `tests`, so the run was a no-op and the file had a real type error. This exact trap is already documented in `kamma/tech.md` (2026-08-21) and I walked into it anyway. For any test file, use `uv run pyright --project /dev/null <file>`, or confirm `filesAnalyzed` is non-zero — a clean report from a checker that analysed nothing is not evidence.
- 2026-08-25 [BEHAVIOR] Justified dropping the OpenAI SDK's automatic retries with "AIManager falls back immediately anyway" — true, but `scripts/extractor/extract_cone.py` builds the provider directly with no fallback chain, so one transient 429 would have ended a whole batch run. When removing a safety net a library provided, enumerate every caller of the code, not just the one whose architecture you had in mind; the reasoning has to hold for the direct callers too. Fix was a `max_retries` parameter defaulting to the fast-fallback behaviour, opted into by the one caller that needs it.
- 2026-08-25 [BEHAVIOR] A survey counted `scripts/extractor/extract_cone.py` among the files matching a provider class name, then wrote "no caller imports a provider class directly" into the spec — the count was read, the file was not. It imported the class and used an attribute the replacement lacks. When a sweep produces a per-file match count, open the non-obvious files before turning the count into a claim.
- 2026-08-25 [POSITIVE] Live-probing all four provider APIs before writing the plan refuted two spec assumptions (NVIDIA answers with RFC 7807 and with plain text, not the nested error shape the other three use; a claimed crash was actually a swallowed exception producing a useless message) and surfaced the defect that justified the whole thread — DeepSeek discarding its own error detail. Ten minutes of real requests beat any amount of reading the code. Probe the real endpoints before speccing against remembered API shapes.
- 2026-08-25 [CONFUSION] User cut in with "i cant make sense of your long-winded answers, they lack a clear call to action" after a correct but sprawling findings summary. Analysis without a next step reads as noise no matter how accurate it is. End every findings or status message with one explicit instruction — what to say or do next — and keep the reasoning to a few lines above it.
- 2026-08-25 [WORKFLOW] A concurrent agent session deleted this thread's uncommitted `research.md` from `kamma/threads/` mid-work; it was never committed, so git could not recover it and it had to be rewritten from context. Shared-tree damage is not limited to source files — thread notes are just as exposed, and an in-progress analysis document is the kind of thing nothing else can reconstruct. Re-check your own thread files exist before finalize, and treat a long-lived uncommitted analysis as at risk.
- 2026-09-01 [BEHAVIOR] agy probe used bool(response) as success check, so a generic greeting from a broken transport passed as healthy and masked the real bug for hours; liveness probes must assert on prompt-specific content, not merely non-empty output
- 2026-09-05 [BEHAVIOR] Specced and shipped a licence `Link` header on three routes without checking whether the header could actually be READ by its intended audience. `Link` is not a CORS-safelisted response header, and the app sets `allow_origins=["*"]` with no `expose_headers`, so cross-origin browser JS got `null` — and two in-repo third-party consumers hit exactly the two HTML routes whose body carries no in-band notice. The independent reviewer found it by sending a real `Origin` request. Shipping a response header is not done when the header is present; verify a representative client can read it under the app's actual middleware config.
- 2026-09-05 [WORKFLOW] `coderabbit review --dir exporter/webapp --include-untracked` reported 0 findings and listed only two reviewed files — the thread's new test file lived under `tests/` and was silently outside the scope. A second run scoped to the tests dir found a real issue. `--dir` scoping is per-invocation and silent about what it excluded: when a change spans source and tests in separate trees, run the reviewer once per tree and check the `reviewedFiles` list against your own changed-files list.
- 2026-09-05 [POSITIVE] The negative assertions in a new test (`"link" not in headers` on out-of-scope routes) were checked for teeth by confirming those routes actually return 200 rather than 404 — a 404 would have made them pass for the wrong reason. CodeRabbit then flagged the audio route for not asserting its status, and the right fix was NOT a hard `== 200`: the audio db is gitignored, so a clean checkout legitimately 404s. Explicit `pytest.skip` with a stated reason keeps the teeth locally without a false failure elsewhere. An absence assertion needs a presence assertion beside it, and environment-dependent fixtures need a skip, not a weakened check.
- 2026-09-05 [BEHAVIOR] The plan listed the subsystem README as the doc to update and missed `docs/technical/api_endpoints.md`, which is the API reference actually linked from the public docs site and which spelled out the exact JSON body being changed. Found by the independent reviewer, not by me. When a change alters a documented response shape, grep the docs tree for the literal field names being changed rather than updating the doc that sits next to the code.
