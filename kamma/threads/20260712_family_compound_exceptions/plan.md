# Plan: family_compound_exceptions (not yet planned)

This thread is **parked**. Its `spec.md` is an explicit placeholder whose "Open
decisions" section is still waiting on the user, so there is nothing to plan yet and no
tasks have been invented here to pretend otherwise.

This file exists only because the kamma spec gate requires every thread directory to
carry both files; it was added on 2026-09-06 by the `20260906_citations` thread, which
was blocked from editing any file outside `kamma/` while it was missing.

## Baseline
- [ ] Not started.

## Tasks
- [ ] Answer the open decisions in `spec.md`, then write a real plan.
