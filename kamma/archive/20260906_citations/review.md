## Thread
- **ID:** 20260906_citations
- **Objective:** Make DPD citable — a versioned citation published everywhere, plus the
  repo metadata GitHub and Zenodo read.

## Files Changed

**dpd-db**
- `LICENSE` — new; canonical CC BY-NC-SA 4.0 text
- `CITATION.cff` — new; now GENERATED by `tools/version.py`
- `tools/version.py` — citation string, db_info upsert, CFF generator, Zenodo DOI lookup
- `tools/docs_update_how_to_cite.py` — new; generates the docs page each build
- `tools/paths.py` — `citation_cff_path`, `docs_how_to_cite_md_path`
- `docs/how_to_cite.md` — new, generated; `docs/index.md`, `docs/license.md`, `mkdocs.yaml` — links + licence fix
- `README.md` — how-to-cite section after the CC badge
- `justfile`, `scripts/bash/makedict.py` — generator wired into `docs-update` and the build
- `exporter/goldendict/export_help.py` — generated `cite` dictionary entry
- `exporter/pdf/pdf_exporter.py`, `templates/front_matter.typ` — citation on the copyright page
- `exporter/webapp/templates/home.html` — version + how-to-cite link in the footer
- `exporter/goldendict/main.py`, `deconstructor/`, `variants/`, `grammar_dict/`, `tools/rss_feed.py`, `pyproject.toml` — author name
- `tests/tools/test_version.py`, `tests/tools/test_docs_update_how_to_cite.py` — new

**dpd-flutter-app**
- `lib/providers/citation_provider.dart`, `lib/widgets/secondary/citation_card.dart` — new
- `lib/widgets/info_popup.dart` — How to Cite menu item
- `test/widgets/citation_card_test.dart`, `README.md`

## Findings

| # | Severity | Location | What | Why | Fix |
|---|----------|----------|------|-----|-----|
| 1 | blocking | `tools/version.py` | Zenodo query sent a pre-encoded `/`; API returned HTTP 500 on every call | Whole DOI automation was dead; `except Exception` reported the 500 as "not published yet", and that false reading was written into the handoff and told to the user | Quoted-phrase query via `urlencode`; verified live |
| 2 | blocking | `tools/version.py` | First hit with a `conceptdoi` accepted, no validation | A free-text search returns other Pāḷi projects; a wrong DOI would be baked into CITATION.cff, dpd.db and every export, permanently | `_hit_is_dpd()` checks `related_identifiers` for the repo; `DOI_PATTERN` validates shape |
| 3 | blocking | `tools/version.py` | Bare `except Exception` | Hid finding 1 completely | Narrowed to URL/timeout/JSON errors, prints the error |
| 4 | major | `tools/version.py` | Network call fired on every run of six CI workflows, never once-only | `config.ini` is gitignored, so CI can never persist a value | Skipped when `CI` is set |
| 5 | major | `README.md` | Hardcoded version already stale within the thread | The exact drift the thread exists to prevent | Version replaced with a placeholder |
| 6 | major | `docs/how_to_cite.md` vs `CITATION.cff` | Disagreed on version in the same commit | Generated from different runs | Page regenerated; both now v0.4.20260906 |
| 7 | major | `export_help.py`, `front_matter.typ` | Built the citation without the DOI | Offline dictionary and PDF would contradict the docs once a DOI exists | Both take `make_citation(version, get_doi())` |
| 8 | major | `plan.md`, `handoff_20260926.md` | Told the user to edit the commented `doi:` line in CITATION.cff | That file is now generated; the edit would be wiped | Rewritten to point at `[version] doi` |
| 9 | major | `CITATION.cff` | `repository-artifact` pointed at `/releases/latest` | Moving target inside a version-specific record | Pinned to `/releases/tag/<version>` |
| 10 | minor | `docs_update_how_to_cite.py` | APA year from `version[5:9]` | Breaks at minor version 10 | Uses `release_date(version)[:4]` |
| 11 | minor | two files | Example lemma/id duplicated | Same drift class | `EXAMPLE_LEMMA`/`EXAMPLE_ID` in `tools/version.py` |
| 12 | nit | `home.html` | `.dpd-version` class had no CSS rule | Dead selector | Class dropped |
| 13 | nit | `citation_card_test.dart` | Param typed `AsyncValue` but collapsed via `valueOrNull`, so the error branch was untested | Misleading signature; error branch had no coverage | Helper now takes the real override callback; a third test covers a throwing lookup |

## Fixes Applied
All 13 findings fixed.
8 new tests added for the Zenodo lookup — the two functions that touch the network had
no test at all, which is why findings 1–3 survived to review.

## Test Evidence
- `uv run pytest tests/` (scope: whole project, 1838 tests) → pass; baseline was 1815, 23 added
- `just typecheck` (scope: repo-wide pyrefly, ~390 files) → 0 errors, matching baseline
- `uv run pyright <each touched file>` (scope: per-file commit gate) → 0 errors
- `uv run mkdocs build` (scope: whole docs site) → 0 errors
- `uvx cffconvert --validate` (scope: generated CITATION.cff, with and without a DOI) → valid
- Live Zenodo API calls (scope: 3 query forms) → old query 500s, new query 200 with 0 hits
- typst compile of the rendered front matter + PDF text extraction (scope: front matter only, not a full dictionary PDF) → citation and version present on page 2
- `flutter analyze` (scope: whole app) → 0 errors; `flutter test` (scope: whole app, 387) → pass
- Permalink resolution via TestClient (scope: one id, 24043) → returns `gacchati 1`, not `aciraṃ`
- `EXAMPLE_ID = 24043` confirmed against the live db as the only `gacchati 1`

## Not Verified
- GitHub's "Cite this repository" button and CC licence badge — impossible before a push.
  The `LICENSE` file prepends a title/copyright/SPDX block, which can defeat GitHub's
  licence detector; check after pushing.
- The `cite` entry inside a real GoldenDict build, and the citation block in a full PDF
  build. Both were verified in isolation only.
- The Flutter card against a freshly exported mobile database (verified against an
  overridden provider, and the `db_info` passthrough was verified by reading the exporter).
- The DOI path end to end — no Zenodo record exists yet. The lookup is proven against
  fabricated payloads and proven not to error against the live API; it has never returned
  a real DOI.
- A full `just makedict` run. Pipeline ordering was verified by reading, not executing.

## Verdict
PASSED
- Review date: 2026-09-06
- Reviewer: independent subagent (fresh context) + CodeRabbit CLI; fixes by the
  implementing agent, re-verified above.
