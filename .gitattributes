# Normalise line endings: store as LF, check out native
* text=auto

# Shell scripts must stay LF even on Windows checkouts
*.sh text eol=lf

# Exclude vendored/generated content from GitHub language stats
resources/** linguist-vendored

# Treat markdown as documentation, not code
*.md linguist-documentation
