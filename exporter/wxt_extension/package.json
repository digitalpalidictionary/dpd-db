{
  "name": "dpd-wxt-extension",
  "version": "1.0.1",
  "private": true,
  "description": "DPD Cross-Browser Extension built with WXT",
  "type": "module",
  "scripts": {
    "dev": "wxt",
    "dev:chrome": "wxt -b chrome",
    "dev:firefox": "wxt -b firefox",
    "build": "wxt build",
    "build:chrome": "wxt build -b chrome",
    "build:firefox": "wxt build -b firefox --mv3",
    "build:safari": "wxt build -b safari",
    "zip": "wxt zip",
    "zip:chrome": "wxt zip -b chrome",
    "zip:firefox": "wxt zip -b firefox --mv3",
    "zip:safari": "wxt zip -b safari",
    "package": "wxt build -b chrome && wxt build -b firefox --mv3 && wxt build -b safari && wxt zip -b chrome && wxt zip -b firefox --mv3 && wxt zip -b safari",
    "compile": "tsc --noEmit",
    "postinstall": "wxt prepare"
  },
  "dependencies": {
    "webextension-polyfill": "^0.12.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/webextension-polyfill": "^0.12.0",
    "typescript": "^5.7.0",
    "wxt": "^0.19.0"
  }
}
